# Site Workspace

- `docs/` stores site-wide notes and architecture documentation.
- `redirects/` stores redirect planning and mapping.
- `schema/` stores structured data notes and schema planning.
- `assets/` stores asset inventories and non-theme site asset notes.

